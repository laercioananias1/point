# Copie para .env e ajuste antes de rodar `docker compose up`
DB_ROOT_PASSWORD=root
JWT_SECRET=change-me-in-.env

# Opcionais em dev (sem eles: e-mail de convite vira log no console em vez
# de enviar de verdade — ver app/services/email.py):
# RESEND_API_KEY=
# RESEND_FROM=Point <onboarding@resend.dev>
# FRONTEND_URL=http://localhost:5173

# Produção usa .env.production (ver .env.production.example e DEPLOY.md),
# não este arquivo.
