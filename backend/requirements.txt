fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pymysql==1.1.1
cryptography==44.0.0
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# bcrypt >=4.1 removeu o atributo `__about__` que o passlib 1.7.4 lê para
# detectar a versão do backend — trava em "error reading bcrypt version".
# Fixado em 4.0.1 até o passlib publicar uma versão compatível.
bcrypt==4.0.1
python-multipart==0.0.20
# Job diário de geração de aulas do mês (pedido do usuário, 2026-08-21) — em
# processo, sem infra externa; ver app/services/scheduler.py.
apscheduler==3.10.4

pytest==8.3.4
httpx==0.28.1
